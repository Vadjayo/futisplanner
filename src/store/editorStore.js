/**
 * editorStore.js
 * Harjoituseditoritila Zustand-storessa. Toteutetaan Vaiheessa 3.
 *
 * Suunniteltu rajapinta:
 *   editorStore.drills[]     — kaikki harjoitussuunnitelmat
 *   editorStore.activeDrill  — avoinna oleva harjoitus
 *   editorStore.elements[]   — kentän elementit (pelaajat, välineet, nuolet)
 *   editorStore.activeTool   — valittu työkalu
 *   editorStore.displayMode  — 'number' | 'jersey'
 *   editorStore.addElement(el)
 *   editorStore.updateElement(id, patch)
 *   editorStore.removeElement(id)
 *   editorStore.undo()
 *   editorStore.redo()
 */

// TODO: Ota Zustand käyttöön ja siirrä EditorApp + DrillCanvas -logiikka tähän
// import { create } from 'zustand'

// Väliaikainen vienti — korvaa oikealla storella Vaiheessa 3
export const editorStore = null
