/**
 * sessionService.js
 * Harjoitussuunnitelmien CRUD-operaatiot Supabasessa. Aiemmin lib/db.js.
 * Siirretään koodi tähän Vaiheessa 2 — tällä hetkellä uudelleenvienti.
 *
 * Rajapinta:
 *   loadDrills(userId)
 *   saveDrill(drill)
 *   updateDrill(id, patch)
 *   deleteDrill(id)
 */

export * from '../lib/db'
