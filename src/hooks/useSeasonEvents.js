/**
 * useSeasonEvents.js
 * Kalenteri-tapahtumien tilan hallinta-hook. Toteutetaan Vaiheessa 2.
 * Erottaa tapahtumaoperaatiot SeasonCalendar.jsx:stä.
 *
 * Suunniteltu rajapinta:
 *   const {
 *     events, loading,
 *     addEvent, updateEvent, deleteEvent, reload,
 *   } = useSeasonEvents(teamId)
 */

// TODO: Siirrä SeasonCalendar.jsx:n tapahtumaoperaatiot tähän Vaiheessa 2
export function useSeasonEvents(_teamId) {
  throw new Error('useSeasonEvents ei ole vielä toteutettu — käytä SeasonCalendar.jsx:n sisäistä tilaa')
}
