/**
 * useSession.js
 * Harjoitussuunnitelmien tilan hallinta-hook. Toteutetaan Vaiheessa 2.
 * Erottaa sessionlogiikan EditorApp.jsx:stä omaan hookkiinsa.
 *
 * Suunniteltu rajapinta:
 *   const {
 *     drills, activeDrill, loading,
 *     addDrill, updateDrill, deleteDrill, selectDrill,
 *   } = useSession(userId)
 */

// TODO: Siirrä EditorApp.jsx:n sessio-logiikka tähän Vaiheessa 2
export function useSession(_userId) {
  throw new Error('useSession ei ole vielä toteutettu — käytä EditorApp.jsx:n sisäistä tilaa')
}
